/**
 * Job Posting Manager - Admin JavaScript
 * Main entry point for admin functionality
 */

// Handle bulk actions
jQuery("#jpm-bulk-form").on("submit", function (e) {
  e.preventDefault();
  // AJAX call similar to above
});

// Form Builder components are loaded separately via wp_enqueue_script
// See job-posting-manager.php for component loading order
